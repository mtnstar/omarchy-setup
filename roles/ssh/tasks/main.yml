---
- name: Install openssh
  pacman:
    name:
      - openssh
    state: present

- name: Ensure environment.d directory exists
  file:
    path: /etc/environment.d
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Set ssh-agent environment for user sessions
  copy:
    dest: /etc/environment.d/10-ssh-agent.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
      SSH_ASKPASS=/usr/lib/seahorse/ssh-askpass
      SSH_ASKPASS_REQUIRE=force

- name: Set ssh-agent environment for login shells
  copy:
    dest: /etc/profile.d/ssh-askpass.sh
    owner: root
    group: root
    mode: "0644"
    content: |
      export SSH_ASKPASS=/usr/lib/seahorse/ssh-askpass
      export SSH_ASKPASS_REQUIRE=force

# openssh ships a socket-activated ssh-agent.socket / ssh-agent.service user
# unit, so we enable the packaged socket instead of shipping our own unit.
- name: Enable ssh-agent socket
  systemd:
    name: ssh-agent.socket
    scope: global
    enabled: true
